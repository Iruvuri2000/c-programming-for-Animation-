#iable_income-MIN_INCOME)
 nclude<stdio.h>
int main()
{
	double taxable_income,tax;
}
ble_income>=MIN_INCOME 1 taxable_income<MAX_INCOME)
 tax=(taclscr();
printf("/n Enter the income:");
scanf("%if",&income);
taxable_income = -150000
if(taxable_income<=0)
{
	printf("/n NO TAX");
}
